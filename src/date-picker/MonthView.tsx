import { defineComponent } from 'vue';

const MonthView = defineComponent({
  name: 'sk-month-view',
  setup() {
    return () => <div class="sk-month-view"></div>;
  },
});

export default MonthView;
